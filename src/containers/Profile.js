import React, { Component } from 'react'

export default class Profile extends Component {

   

    render() {
        return (
            <div>
            </div>
        )
    }
}

/* <h3>Hello {this.props.username}</h3>
{this.props.user_species.map(species => <div key={species.id}> {species.common_name} <img src={species.image} alt=""/></div>)}  */
